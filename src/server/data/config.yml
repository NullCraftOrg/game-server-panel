# 绑定的IP地址 默认:localhost
ip: localhost

# 端口 默认：9119
port: 9119

# 是否记录运行日志到 logs/app.log 中
app-log-to-file: false

# 生成JWT的盐值，建议修改为随机字符串
jwt-salt: "F0R-T35T"

# JWT令牌过期时间，支持如 7d, 24h, 60m 等格式
# 具体语法可参考：https://github.com/vercel/ms
token-expires: "7d"
